{
  "name": "swa-platform",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "smoke": "tsx scripts/smoke.ts",
    "typecheck": "pnpm -r --if-present typecheck",
    "dev:platform": "pnpm --filter platform dev",
    "dev:worker": "pnpm --filter @swa/renderer dev"
  },
  "devDependencies": {
    "@swa/db": "workspace:*",
    "@swa/lib-llm": "workspace:*",
    "@swa/lib-tts": "workspace:*",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "zod": "^3.23.0"
  }
}
